[project]
name = "younilab-resource-catalog-domain"
version = "0.1.0"
description = "Customer and SEO task master-data domain."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["uv_build>=0.7.12,<0.8.0"]
build-backend = "uv_build"
